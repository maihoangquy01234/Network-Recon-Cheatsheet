# 🛠️ Quy Trình Tấn Công Mạng & Thu Thập Thông Tin (Recon & Enumeration)

Đây là bảng tổng hợp các công cụ và lệnh cần thiết cho giai đoạn thu thập thông tin và liệt kê dịch vụ, được tổ chức theo quy trình chuẩn của Penetration Testing.

| Giai đoạn | Mục tiêu | Công cụ (Tool) | Các bước thực hiện & Lệnh mẫu (Commands) |
| :--- | :--- | :--- | :--- |
| **1. Thu thập thông tin thụ động (OSINT)** | Tìm kiếm tên miền phụ, email, hostnames từ các nguồn công khai. | **amass**, **subfinder**, **theHarvester** | 1. **Tìm tên miền phụ (Subdomains):**<br>`amass enum -d target.com -o amass_subdomains.txt`<br>`subfinder -d target.com -o subfinder_subdomains.txt`<br>2. **Tìm Email/Hostnames:**<br>`theharvester -d target.com -l 500 -b all` |
| **2. Phân tích DNS** | Thu thập thông tin DNS chi tiết, tìm kiếm các Host Record, kiểm tra Zone Transfer. | **fierce**, **dnsenum** | 1. **Kiểm tra Zone Transfer & Subdomains cơ bản:**<br>`fierce -dns target.com`<br>2. **Liệt kê DNS toàn diện:**<br>`dnsenum target.com` |
| **3. Khám phá & Quét cổng nhanh** | Xác định nhanh chóng các IP đang hoạt động và các cổng TCP/UDP đang mở. | **masscan**, **rustscan** | 1. **Quét tốc độ cao (toàn bộ cổng):**<br>`masscan -p1-65535 10.10.10.0/24 --rate 5000`<br>2. **Quét nhanh, chuyển kết quả cho Nmap:**<br>`rustscan -a 10.10.10.10 --ulimit 5000 -- -sV -sC` |
| **4. Quét sâu dịch vụ & OS** | Xác định chi tiết dịch vụ, phiên bản đang chạy và hệ điều hành của mục tiêu. | **nmap** | 1. **Quét toàn diện (Service, Version, OS, Script):**<br>`nmap -sS -sV -O -p- 10.10.10.10 -oA full_scan`<br>2. **Quét dựa trên cổng mở từ Rustscan:**<br>`nmap -sV -sC -p <ports_from_rustscan> 10.10.10.10` |
| **5. Liệt kê dịch vụ (Enumeration)** | Khai thác thông tin từ các dịch vụ cụ thể như SMB/Samba. | **enum4linux-ng**, **netexec** | 1. **Liệt kê SMB/Samba:**<br>`enum4linux-ng 10.10.10.10`<br>2. **Kiểm tra thông tin đăng nhập/Chia sẻ SMB:**<br>`netexec smb 10.10.10.10 -L`<br>`netexec smb 10.10.10.10 -u user -p pass` |
| **6. Quét lỗ hổng cơ bản** | Kiểm tra các cấu hình sai hoặc lỗ hổng đã biết trên các mục tiêu đã tìm thấy. | **nuclei** | 1. **Tạo danh sách mục tiêu:** Ghép các IP/Host vào file `targets.txt`<br>2. **Chạy Nuclei (ví dụ: quét lỗ hổng cơ bản):**<br>`nuclei -l targets.txt -t cves/ -t default-logins/ -o nuclei_results.txt` |
| **7. Tự động hóa & Báo cáo (Tùy chọn)** | Tự động hóa toàn bộ quy trình Recon cơ bản và lưu kết quả. | **autorecon** | 1. **Chạy tự động hóa toàn bộ quy trình:**<br>`autorecon 10.10.10.10` |
| **8. Tấn công cục bộ (LAN)** | Thu thập thông tin đăng nhập trong mạng nội bộ (LAN) bằng cách giả mạo dịch vụ phân giải tên. | **responder** | 1. **Chạy Responder:**<br>`responder -I eth0` |
